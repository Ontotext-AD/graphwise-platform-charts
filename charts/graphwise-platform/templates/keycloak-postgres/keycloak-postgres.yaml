{{- if .Values.keycloak_postgres.enabled }}
#
# PostgreSQL cluster for Keycloak.
# See https://cloudnative-pg.io/documentation/current/cloudnative-pg.v1/
#
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ .Values.keycloak_postgres.name }}
  namespace: {{ .Values.keycloak.namespace }}
  labels:
    {{- include "graphwise-platform.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
spec:
  imageName: {{ include "graphwise-platform.keycloak_postgres.image" . }}
  instances: {{ .Values.keycloak_postgres.replicas }}
  bootstrap:
    initdb:
      database: {{ .Values.keycloak_postgres.database.name }}
      owner: {{ .Values.keycloak_postgres.database.owner }}
  {{- with .Values.keycloak_postgres.postgresql }}
  postgresql:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  storage:
    {{- if .Values.keycloak_postgres.storage.storageClassName }}
    storageClass: {{ .Values.keycloak_postgres.storage.storageClassName }}
    {{- end }}
    size: {{ .Values.keycloak_postgres.storage.size }}
  {{- with .Values.keycloak_postgres.resources }}
  resources: {{ toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.keycloak_postgres.serviceAccountTemplate }}
  serviceAccountTemplate: {{ tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
  {{- with .Values.global.imagePullSecrets }}
  imagePullSecrets:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
  {{- if .Values.keycloak_postgres.backups.enabled }}
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: {{ .Values.keycloak_postgres.name }}
  {{- end }}
  {{- with .Values.keycloak_postgres.nodeSelector }}
  affinity:
    nodeSelector: {{ tpl (toYaml .) $ | nindent 6 }}
  {{- end }}
  {{- if and (gt (int .Values.keycloak_postgres.replicas) 1) (not .Values.keycloak_postgres.topologySpreadConstraints) }}
  topologySpreadConstraints:
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          cnpg.io/cluster: keycloak-postgres
    - topologyKey: kubernetes.io/hostname
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          cnpg.io/cluster: keycloak-postgres
  {{- end }}
  {{- with .Values.elasticsearch.topologySpreadConstraints }}
  topologySpreadConstraints:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
{{- end }}
